
<%= form_with model: @profile do |form| %>
  <%= form.label :title %><br />
  <%= form.text_field :title, :required => true %><br><br>
  <%= form.label :description %><br />
  <%= form.text_area :description, size: "60x10", :required => true %><br>
  <% if @profile.photo.attached? %>
    <%= cl_image_tag @profile.photo.url, cloud_name: $cloud_name %><br>
    <%= form.label "Change Photo" %><br />
    <%= form.file_field :photo %><br /><br />
  <% else %>
    <%= form.label "Add Your Photo" %><br />
    <%= form.file_field :photo %><br /><br />
  <% end %>
  <%= form.submit %><br><br>
<% end %>
